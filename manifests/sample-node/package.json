{
  "name": "sample-node",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "cd server && pnpm install && cd ../ui && pnpm install && pnpm build",
    "start": "node server/server.js"
  }
}
